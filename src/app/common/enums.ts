export enum EnumStatus {
  ALL,
  DRAFT,
  SUBMITTED,
  PENDING,
  RETURNED,
  REJECTED,
  APPROVED,
  CONSULT,
  RESPONEDED
}

export enum EnumRequestType {
  New = 1,
  Success = 2
}

export enum EnumRole {
  INITIATOR,
  REVIEWER,
  APPROVER,
  CONSULT
}